import { HighlightedDomain } from './highlighted-domain.model';

describe('HighlightedDomain', () => {
  it('should create an instance', () => {
    expect(new HighlightedDomain()).toBeTruthy();
  });
});
